{
  "schema_version": "2026-07-01",
  "name": "libhidapi/libhidapi",
  "version": "0.14.0",
  "description": "PHP FFI wrapper for the HIDAPI USB HID library.",
  "authors": [
    {
      "name": "HIDAPI maintainers"
    }
  ],
  "license": "BSD-3-Clause",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libhidapi\\Libhidapi",
  "examples": [
    "use Pnlx\\Libhidapi\\Libhidapi;\nuse Pnlx\\Runtime;\nuse function Pnlx\\Util\\is_null;\n\n$runtime = new Runtime(__DIR__);\n$libhidapi = $runtime->load(Libhidapi::class);\n\n// Initialize HIDAPI and enumerate connected USB HID devices\n$rc = $libhidapi->hid_init();\nif ($rc === 0) {\n    // Enumerate all HID devices (vendor 0, product 0 = all)\n    $devs = $libhidapi->hid_enumerate(0, 0);\n    $cur = $devs;\n    while (!is_null($cur)) {\n        echo \"Device: \" . $cur->manufacturer_string . \"\\n\";\n        $cur = $cur->next;\n    }\n    $libhidapi->hid_free_enumeration($devs);\n    $libhidapi->hid_exit();\n}"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install hidapi"],
      "checkIfExists": ["brew list hidapi"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libhidapi-dev"],
      "checkIfExists": ["pkg-config --exists hidapi-libusb"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libhidapi": {
      "symbol_prefix": "hid",
      "library_names": [
        "libhidapi.dylib",
        "libhidapi.so",
        "hidapi.dll"
      ],
      "header_names": [
        "hidapi/hidapi.h"
      ],
      "version": ">=0.10.0 & <1.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
