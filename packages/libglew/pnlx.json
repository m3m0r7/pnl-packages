{
  "schema_version": "2026-07-01",
  "name": "libglew/libglew",
  "version": "2.2.0",
  "description": "PHP FFI wrapper for the GLEW OpenGL extension loader.",
  "authors": [
    {
      "name": "GLEW maintainers"
    }
  ],
  "license": "BSD-3-Clause",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libglew\\Libglew",
  "examples": [
    "use Pnlx\\Libglew\\Libglew;\nuse Pnlx\\Runtime;\n\n$runtime = new Runtime(__DIR__);\n$libglew = $runtime->load(Libglew::class);\n\n// glewGetString returns a GLubyte* (string) with GLEW version info.\n// A valid OpenGL context must be current before calling glewInit().\n$versionStr = $libglew->glewGetString(1); // GLEW_VERSION = 1\necho \"GLEW version: \" . $versionStr . \"\\n\";\n\n// After glewInit() succeeds (rc == GLEW_OK == 0), extension checks become available.\n// $rc = $libglew->glewInit();\n// $supported = $libglew->glewIsSupported('GL_ARB_vertex_array_object');"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install glew"],
      "checkIfExists": ["brew list glew"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libglew-dev"],
      "checkIfExists": ["pkg-config --exists glew"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libGLEW": {
      "symbol_prefix": "glew",
      "library_names": [
        "libGLEW.dylib",
        "libGLEW.so",
        "glew32.dll"
      ],
      "header_names": [
        "GL/glew.h"
      ],
      "version": ">=2.0.0 & <3.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
