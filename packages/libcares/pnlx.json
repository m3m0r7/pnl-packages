{
  "schema_version": "2026-07-01",
  "name": "libcares/libcares",
  "version": "1.28.1",
  "description": "PHP FFI wrapper for the c-ares async DNS library.",
  "authors": [
    {
      "name": "c-ares maintainers"
    }
  ],
  "license": "MIT",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libcares\\Libcares",
  "examples": [
    "use Pnlx\\Libcares\\Libcares;\nuse Pnlx\\Runtime;\n\n$runtime = new Runtime(__DIR__);\n$libcares = $runtime->load(Libcares::class);\n\n// Initialise the c-ares library and print its version string\n$rc = $libcares->ares_library_init(1); // ARES_LIB_INIT_ALL = 1\nif ($rc !== 0) {\n    throw new \\RuntimeException(\"ares_library_init failed: {$rc}\");\n}\n\n$versionNumber = $runtime->allocator()->make(\\Pnlx\\FFI\\AllocationType::Int);\n$versionStr    = $libcares->ares_version($versionNumber);\necho \"c-ares version: {$versionStr}\\n\";\n\n// Asynchronous DNS: $libcares->ares_init($channel) then $libcares->ares_gethostbyname(...)\n$libcares->ares_library_cleanup();"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install c-ares"],
      "checkIfExists": ["brew list c-ares"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libc-ares-dev"],
      "checkIfExists": ["pkg-config --exists libcares"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libcares": {
      "symbol_prefix": "ares",
      "library_names": [
        "libcares.dylib",
        "libcares.so",
        "cares.dll"
      ],
      "header_names": [
        "ares.h"
      ],
      "version": ">=1.0.0 & <2.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
