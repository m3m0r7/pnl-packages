{
  "schema_version": "2026-07-01",
  "name": "libglfw/libglfw",
  "version": "3.4.0",
  "description": "PHP FFI wrapper for the GLFW windowing library.",
  "authors": [
    {
      "name": "GLFW maintainers"
    }
  ],
  "license": "Zlib",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libglfw\\Libglfw",
  "examples": [
    "use Pnlx\\Libglfw\\Libglfw;\nuse Pnlx\\Runtime;\n\n$runtime = new Runtime(__DIR__);\n$libglfw = $runtime->load(Libglfw::class);\n\n// Query GLFW version string before initializing (always safe)\n$versionStr = $libglfw->glfwGetVersionString();\necho \"GLFW: \" . $versionStr . \"\\n\";\n\n// Initialize GLFW, create a window, then terminate\nif ($libglfw->glfwInit()) {\n    $libglfw->glfwWindowHint(0x00022001, 1); // GLFW_CONTEXT_VERSION_MAJOR = 3\n    // $window = $libglfw->glfwCreateWindow(800, 600, 'Hello', null, null);\n    $libglfw->glfwTerminate();\n}"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install glfw"],
      "checkIfExists": ["brew list glfw"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libglfw3-dev"],
      "checkIfExists": ["pkg-config --exists glfw3"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "glfw3": {
      "symbol_prefix": "glfw",
      "library_names": [
        "libglfw.dylib",
        "libglfw.so",
        "glfw3.dll"
      ],
      "header_names": [
        "GLFW/glfw3.h"
      ],
      "version": ">=3.3.0 & <4.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
