{
  "schema_version": "2026-07-01",
  "name": "libargon2/libargon2",
  "version": "1.0.0",
  "description": "PHP FFI wrapper for the Argon2 password hashing library.",
  "authors": [
    {
      "name": "Argon2 maintainers"
    }
  ],
  "license": "Apache-2.0",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libargon2\\Libargon2",
  "examples": [
    "use Pnlx\\Libargon2\\Libargon2;\nuse Pnlx\\Runtime;\n\n$runtime = new Runtime(__DIR__);\n$libargon2 = $runtime->load(Libargon2::class);\n\n// Hash a password with Argon2id and verify it\n// argon2id_hash_encoded writes into a caller-supplied buffer (char *encoded)\n$password   = 'hunter2';\n$salt       = 'somesalt12345678'; // 16 bytes\n$encodedLen = 512;\n$encodedBuf = $runtime->allocator()->make(\\Pnlx\\FFI\\AllocationType::VoidPointer);\n\n$rc = $libargon2->argon2id_hash_encoded(\n    2,              // t_cost: iterations\n    1 << 16,        // m_cost: memory (64 MiB)\n    1,              // parallelism\n    $password, strlen($password),\n    $salt, strlen($salt),\n    32,             // hash length in bytes\n    $encodedBuf, $encodedLen\n);\n// ARGON2_OK == 0\necho $rc === 0 ? \"hash ok\\n\" : 'error: ' . $libargon2->argon2_error_message($rc) . \"\\n\";\n// Verify: $libargon2->argon2id_verify($encodedBuf, $password, strlen($password))"
  ],
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libargon2": {
      "symbol_prefix": "argon2",
      "library_names": [
        "libargon2.dylib",
        "libargon2.so",
        "argon2.dll"
      ],
      "header_names": [
        "argon2.h"
      ],
      "version": ">=0.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
