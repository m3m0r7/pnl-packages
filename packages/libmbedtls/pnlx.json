{
  "schema_version": "2026-07-01",
  "name": "libmbedtls/libmbedtls",
  "version": "3.6.0",
  "description": "PHP FFI wrapper for Mbed TLS.",
  "authors": [
    {
      "name": "Mbed TLS maintainers"
    }
  ],
  "license": "Apache-2.0",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libmbedtls\\Libmbedtls",
  "examples": [
    "use Pnlx\\Libmbedtls\\Libmbedtls;\nuse Pnlx\\Runtime;\n\n$runtime = new Runtime(__DIR__);\n$libmbedtls = $runtime->load(Libmbedtls::class);\n\n// Compute an SHA-256 digest using the Mbed TLS MD (message digest) API.\n$ctx = $libmbedtls->mbedtls_md_info_from_type(6); // MBEDTLS_MD_SHA256 = 6\n$mdCtx = $runtime->allocator()->make(\\Pnlx\\FFI\\AllocationType::VoidPointer);\n$libmbedtls->mbedtls_md_init($mdCtx);\n$libmbedtls->mbedtls_md_setup($mdCtx, $ctx, 0);\n$libmbedtls->mbedtls_md_starts($mdCtx);\n$msg = 'hello';\n$libmbedtls->mbedtls_md_update($mdCtx, $msg, strlen($msg));\n$out = str_repeat(\"\\0\", 32);\n$libmbedtls->mbedtls_md_finish($mdCtx, $out);\necho bin2hex($out) . PHP_EOL; // SHA-256 of \"hello\"\n$libmbedtls->mbedtls_md_free($mdCtx);"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install mbedtls"],
      "checkIfExists": ["brew list mbedtls"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libmbedtls-dev"],
      "checkIfExists": ["pkg-config --exists mbedtls"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libmbedtls": {
      "symbol_prefix": "mbedtls",
      "library_names": [
        "libmbedtls.dylib",
        "libmbedtls.so",
        "mbedtls.dll"
      ],
      "header_names": [
        "mbedtls/ssl.h"
      ],
      "version": ">=3.0.0 & <4.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
