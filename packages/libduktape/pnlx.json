{
  "schema_version": "2026-07-01",
  "name": "libduktape/libduktape",
  "version": "2.7.0",
  "description": "PHP FFI wrapper for the Duktape JavaScript engine.",
  "authors": [
    {
      "name": "Duktape maintainers"
    }
  ],
  "license": "MIT",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libduktape\\Libduktape",
  "examples": [
    "use Pnlx\\Libduktape\\Libduktape;\nuse Pnlx\\Runtime;\nuse function Pnlx\\Util\\is_null;\n\n$runtime = new Runtime(__DIR__);\n$libduktape = $runtime->load(Libduktape::class);\n\n// Create a Duktape heap and evaluate a JavaScript expression.\n$ctx = $libduktape->duk_create_heap_default();\nif (is_null($ctx)) {\n    throw new \\RuntimeException('Failed to create Duktape heap');\n}\n\n$libduktape->duk_eval_string($ctx, \"1 + 2\");\n$result = $libduktape->duk_get_number($ctx, -1);\necho \"1 + 2 = \" . $result . PHP_EOL;   // 3\n\n$libduktape->duk_pop($ctx);\n$libduktape->duk_destroy_heap($ctx);"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install duktape"],
      "checkIfExists": ["brew list duktape"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libduktape-dev"],
      "checkIfExists": ["ldconfig -p | grep -q libduktape.so"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libduktape": {
      "symbol_prefix": "duk",
      "library_names": [
        "libduktape.dylib",
        "libduktape.so",
        "duktape.dll"
      ],
      "header_names": [
        "duktape.h"
      ],
      "version": ">=2.0.0 & <3.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
