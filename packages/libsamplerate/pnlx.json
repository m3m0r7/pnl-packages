{
  "schema_version": "2026-07-01",
  "name": "libsamplerate/libsamplerate",
  "version": "0.2.2",
  "description": "PHP FFI wrapper for libsamplerate sample-rate conversion.",
  "authors": [
    {
      "name": "libsamplerate maintainers"
    }
  ],
  "license": "BSD-2-Clause",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libsamplerate\\Libsamplerate",
  "examples": [
    "use Pnlx\\Libsamplerate\\Libsamplerate;\nuse Pnlx\\Runtime;\nuse function Pnlx\\Util\\is_null;\n\n$runtime = new Runtime(__DIR__);\n$libsamplerate = $runtime->load(Libsamplerate::class);\n\n// Print library version and available converter names\necho $libsamplerate->src_get_version() . \"\\n\";\necho $libsamplerate->src_get_name(0) . \"\\n\"; // SRC_SINC_BEST_QUALITY\n\n// Create a resampler (SRC_LINEAR = 2), 1 channel\n$errCode = $runtime->allocator()->make(\\Pnlx\\FFI\\AllocationType::Int);\n$state = $libsamplerate->src_new(2, 1, $errCode);\nif (is_null($state)) {\n    throw new \\RuntimeException('src_new failed: ' . $libsamplerate->src_strerror($errCode[0]));\n}\n\n// Convert: fill an SRC_DATA struct, then $libsamplerate->src_process($state, $srcData)\n// Ratio example — 44100 Hz -> 22050 Hz: $srcData->src_ratio = 0.5\n\n$libsamplerate->src_delete($state);"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install libsamplerate"],
      "checkIfExists": ["brew list libsamplerate"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libsamplerate0-dev"],
      "checkIfExists": ["pkg-config --exists samplerate"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libsamplerate": {
      "symbol_prefix": "src",
      "library_names": [
        "libsamplerate.dylib",
        "libsamplerate.so",
        "samplerate.dll"
      ],
      "header_names": [
        "samplerate.h"
      ],
      "version": ">=0.2.0 & <1.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
