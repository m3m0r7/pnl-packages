{
  "schema_version": "2026-07-01",
  "name": "libssh/libssh",
  "version": "0.10.6",
  "description": "PHP FFI wrapper for the libssh SSH library.",
  "authors": [
    {
      "name": "libssh maintainers"
    }
  ],
  "license": "LGPL-2.1-only",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libssh\\Libssh",
  "examples": [
    "use Pnlx\\Libssh\\Libssh;\nuse Pnlx\\Runtime;\nuse function Pnlx\\Util\\is_null;\n\n$runtime = new Runtime(__DIR__);\n$libssh = $runtime->load(Libssh::class);\n\n// Create a new SSH session.\n$session = $libssh->ssh_new();\nif (is_null($session)) { throw new \\RuntimeException('ssh_new failed'); }\n\n// Configure the host before connecting.\n$libssh->ssh_options_set($session, 0 /* SSH_OPTIONS_HOST */, 'example.com');\n\n// Connect, authenticate, run commands, then free the session.\n// $libssh->ssh_connect($session);\n$libssh->ssh_free($session);"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install libssh"],
      "checkIfExists": ["brew list libssh"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libssh-dev"],
      "checkIfExists": ["pkg-config --exists libssh"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libssh": {
      "symbol_prefix": "ssh",
      "library_names": [
        "libssh.dylib",
        "libssh.so",
        "ssh.dll"
      ],
      "header_names": [
        "libssh/libssh.h"
      ],
      "version": ">=0.9.0 & <1.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
