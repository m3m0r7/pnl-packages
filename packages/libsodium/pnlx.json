{
  "schema_version": "2026-07-01",
  "name": "libsodium/libsodium",
  "version": "1.0.19",
  "description": "PHP FFI wrapper for libsodium cryptography.",
  "authors": [
    {
      "name": "libsodium maintainers"
    }
  ],
  "license": "ISC",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libsodium\\Libsodium",
  "examples": [
    "use Pnlx\\Libsodium\\Libsodium;\nuse Pnlx\\Runtime;\n\n$runtime = new Runtime(__DIR__);\n$libsodium = $runtime->load(Libsodium::class);\n\n// Initialise the library (must be called once before any crypto operation).\n$libsodium->sodium_init();\n\n// Print the library version string.\n$version = $libsodium->sodium_version_string();\necho $version . PHP_EOL;\n\n// Key and nonce size constants are returned as plain integers.\n$keyBytes   = $libsodium->crypto_secretbox_keybytes();\n$nonceBytes = $libsodium->crypto_secretbox_noncebytes();\necho \"secretbox key={$keyBytes} nonce={$nonceBytes}\" . PHP_EOL;\n// Use $libsodium->crypto_secretbox_easy(...) for authenticated encryption."
  ],
  "installation": {
    "darwin": {
      "install": ["brew install libsodium"],
      "checkIfExists": ["brew list libsodium"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libsodium-dev"],
      "checkIfExists": ["pkg-config --exists libsodium"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libsodium": {
      "library_names": [
        "libsodium.dylib",
        "libsodium.so",
        "libsodium.dll"
      ],
      "header_names": [
        "sodium.h"
      ],
      "version": ">=1.0.0 & <2.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
