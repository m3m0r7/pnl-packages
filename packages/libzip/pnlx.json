{
  "schema_version": "2026-07-01",
  "name": "libzip/libzip",
  "version": "1.10.1",
  "description": "PHP FFI wrapper for libzip archive handling.",
  "authors": [
    {
      "name": "libzip maintainers"
    }
  ],
  "license": "BSD-3-Clause",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libzip\\Libzip",
  "examples": [
    "use Pnlx\\Libzip\\Libzip;\nuse Pnlx\\Runtime;\nuse function Pnlx\\Util\\is_null;\n\n$runtime = new Runtime(__DIR__);\n$libzip = $runtime->load(Libzip::class);\n\n// zip_open() opens a ZIP archive; zip_get_num_entries() counts its entries.\n$errp = $runtime->allocator()->make(\\Pnlx\\FFI\\AllocationType::Int);\n$zip = $libzip->zip_open(\"/path/to/archive.zip\", 0, $errp);\nif (is_null($zip)) {\n    echo \"zip_open failed, error: \" . $errp[0] . \"\\n\";\n} else {\n    $count = $libzip->zip_get_num_entries($zip, 0);\n    echo \"Entries: $count\\n\";\n    $libzip->zip_close($zip);\n}"
  ],
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libzip": {
      "symbol_prefix": "zip",
      "library_names": [
        "libzip.dylib",
        "libzip.so",
        "zip.dll"
      ],
      "header_names": [
        "zip.h"
      ],
      "version": ">=1.0.0 & <2.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
