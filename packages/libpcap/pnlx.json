{
  "schema_version": "2026-07-01",
  "name": "libpcap/libpcap",
  "version": "1.10.4",
  "description": "PHP FFI wrapper for libpcap packet capture.",
  "authors": [
    {
      "name": "libpcap maintainers"
    }
  ],
  "license": "BSD-3-Clause",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libpcap\\Libpcap",
  "examples": [
    "use Pnlx\\Libpcap\\Libpcap;\nuse Pnlx\\Runtime;\nuse function Pnlx\\Util\\is_null;\n\n$runtime = new Runtime(__DIR__);\n$libpcap = $runtime->load(Libpcap::class);\n\n// Query the library version string (char* → PHP string)\n$version = $libpcap->pcap_lib_version();\necho \"libpcap version: $version\\n\";\n\n// Look up the default network device for capture\n$errbuf = $runtime->allocator()->make(\\Pnlx\\FFI\\AllocationType::VoidPointer);\n$handle = $libpcap->pcap_open_live('eth0', 65535, 1, 1000, $errbuf);\nif (is_null($handle)) {\n    echo \"Could not open device\\n\";\n} else {\n    // ... capture packets with pcap_loop() / pcap_next() ...\n    $libpcap->pcap_close($handle);\n}"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install libpcap"],
      "checkIfExists": ["brew list libpcap"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libpcap-dev"],
      "checkIfExists": ["ldconfig -p | grep -q libpcap"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libpcap": {
      "symbol_prefix": "pcap",
      "library_names": [
        "libpcap.dylib",
        "libpcap.so",
        "pcap.dll"
      ],
      "header_names": [
        "pcap.h"
      ],
      "version": ">=1.0.0 & <2.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
