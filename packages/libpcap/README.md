# libpcap Package

Package: `libpcap/libpcap`

Base library: libpcap

License: BSD-3-Clause

Install with `pnl`:

```sh
pnl install https://github.com/m3m0r7/pnl-packages/tree/main/packages/libpcap
```

The package requires libpcap headers and library files to be available through the system, Homebrew, pkg-config, or configured search paths.
