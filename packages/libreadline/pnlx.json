{
  "schema_version": "2026-07-01",
  "name": "libreadline/libreadline",
  "version": "8.2",
  "description": "PHP FFI wrapper for GNU Readline line editing.",
  "authors": [
    {
      "name": "GNU Readline maintainers"
    }
  ],
  "license": "GPL-3.0-or-later",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libreadline\\Libreadline",
  "examples": [
    "use Pnlx\\Libreadline\\Libreadline;\nuse Pnlx\\Runtime;\n\n$runtime = new Runtime(__DIR__);\n$libreadline = $runtime->load(Libreadline::class);\n\n// Initialise the Readline library explicitly\n$libreadline->rl_initialize();\n\n// Read a line from stdin with an interactive prompt.\n// readline() returns a PHP string (malloc-allocated in C); add it to history\n// so the user can recall it with the up-arrow key, then free the C buffer.\n$line = $libreadline->readline('Enter command: ');\nif ($line !== null) {\n    echo \"You entered: {$line}\\n\";\n    $libreadline->add_history($line);\n    $libreadline->rl_free($line);\n}\n// For batch history save/restore: $libreadline->write_history('~/.myapp_history')\n//                               / $libreadline->read_history('~/.myapp_history')"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install readline"],
      "checkIfExists": ["brew list readline"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libreadline-dev"],
      "checkIfExists": ["ldconfig -p | grep -q libreadline"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "readline": {
      "symbol_prefix": "rl",
      "library_names": [
        "libreadline.dylib",
        "libreadline.so",
        "readline.dll"
      ],
      "header_names": [
        "readline/readline.h",
        "readline/history.h"
      ],
      "version": ">=8.0.0 & <9.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
