{
  "schema_version": "2026-07-01",
  "name": "libtcc/libtcc",
  "version": "0.9.27",
  "description": "PHP FFI wrapper for the TinyCC compiler.",
  "authors": [
    {
      "name": "TinyCC maintainers"
    }
  ],
  "license": "LGPL-2.1-or-later",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libtcc\\Libtcc",
  "examples": [
    "use Pnlx\\Libtcc\\Libtcc;\nuse Pnlx\\Runtime;\nuse function Pnlx\\Util\\is_null;\n\n$runtime = new Runtime(__DIR__);\n$libtcc = $runtime->load(Libtcc::class);\n\n// Create a TinyCC compilation context.\n$tcc = $libtcc->tcc_new();\nif (is_null($tcc)) { throw new \\RuntimeException('tcc_new failed'); }\n\n// Compile a C string in memory and run it.\n$libtcc->tcc_set_output_type($tcc, 1 /* TCC_OUTPUT_MEMORY */);\n$libtcc->tcc_compile_string($tcc, 'int add(int a,int b){return a+b;}');\n$libtcc->tcc_relocate($tcc);\n// $fn = $libtcc->tcc_get_symbol($tcc, 'add'); // returns FFI\\CData pointer\n$libtcc->tcc_delete($tcc);"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install tinycc"],
      "checkIfExists": ["brew list tinycc"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libtcc-dev"],
      "checkIfExists": ["ldconfig -p | grep -q libtcc.so"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libtcc": {
      "symbol_prefix": "tcc",
      "library_names": [
        "libtcc.dylib",
        "libtcc.so",
        "tcc.dll"
      ],
      "header_names": [
        "libtcc.h"
      ],
      "version": ">=0.9.0 & <1.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
