{
  "schema_version": "2026-07-01",
  "name": "libheif/libheif",
  "version": "1.17.6",
  "description": "PHP FFI wrapper for libheif HEIF/AVIF codec.",
  "authors": [
    {
      "name": "libheif maintainers"
    }
  ],
  "license": "LGPL-3.0-or-later",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libheif\\Libheif",
  "examples": [
    "use Pnlx\\Libheif\\Libheif;\nuse Pnlx\\Runtime;\nuse function Pnlx\\Util\\is_null;\n\n$runtime = new Runtime(__DIR__);\n$libheif = $runtime->load(Libheif::class);\n\n// Query libheif version string\n$version = $libheif->heif_get_version();\necho \"libheif version: \" . $version . \"\\n\";\n\n// Allocate a context, load a HEIF file, then release\n$ctx = $libheif->heif_context_alloc();\nif (!is_null($ctx)) {\n    // $err = $libheif->heif_context_read_from_file($ctx, 'image.heic', null);\n    $libheif->heif_context_free($ctx);\n}"
  ],
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libheif": {
      "symbol_prefix": "heif",
      "library_names": [
        "libheif.dylib",
        "libheif.so",
        "heif.dll"
      ],
      "header_names": [
        "libheif/heif.h"
      ],
      "version": ">=1.0.0 & <2.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
