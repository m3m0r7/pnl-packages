{
  "schema_version": "2026-07-01",
  "name": "libsndfile/libsndfile",
  "version": "1.2.2",
  "description": "PHP FFI wrapper for libsndfile audio I/O.",
  "authors": [
    {
      "name": "libsndfile maintainers"
    }
  ],
  "license": "LGPL-2.1-or-later",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libsndfile\\Libsndfile",
  "examples": [
    "use Pnlx\\Libsndfile\\Libsndfile;\nuse Pnlx\\Runtime;\nuse function Pnlx\\Util\\is_null;\n\n$runtime = new Runtime(__DIR__);\n$libsndfile = $runtime->load(Libsndfile::class);\n\n// Get the libsndfile version string.\n$version = $libsndfile->sf_version_string();\necho $version . PHP_EOL;\n\n// Open a sound file (read-only). SF_INFO must be allocated as a struct.\n// $info = ...; // allocate SF_INFO via $runtime->allocator()\n// $sndfile = $libsndfile->sf_open('/path/to/audio.wav', 0x10 /* SFM_READ */, $info);\n// if (!is_null($sndfile)) { $libsndfile->sf_close($sndfile); }"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install libsndfile"],
      "checkIfExists": ["brew list libsndfile"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libsndfile1-dev"],
      "checkIfExists": ["pkg-config --exists sndfile"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "sndfile": {
      "symbol_prefix": "sf",
      "library_names": [
        "libsndfile.dylib",
        "libsndfile.so",
        "sndfile.dll"
      ],
      "header_names": [
        "sndfile.h"
      ],
      "version": ">=1.0.0 & <2.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
