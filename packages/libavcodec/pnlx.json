{
  "schema_version": "2026-07-01",
  "name": "libavcodec/libavcodec",
  "version": "61.3.100",
  "description": "PHP FFI wrapper for FFmpeg libavcodec.",
  "authors": [
    {
      "name": "FFmpeg libavcodec maintainers"
    }
  ],
  "license": "LGPL-2.1-or-later",
  "entrypoint": "src/generated/index.php",
  "class": "Pnlx\\Libavcodec\\Libavcodec",
  "examples": [
    "use Pnlx\\Libavcodec\\Libavcodec;\nuse Pnlx\\Runtime;\n\n$runtime = new Runtime(__DIR__);\n$libavcodec = $runtime->load(Libavcodec::class);\n\n// Print the libavcodec version string\n$version = $libavcodec->avcodec_version();\n$major   = ($version >> 16) & 0xff;\n$minor   = ($version >>  8) & 0xff;\n$micro   = $version & 0xff;\necho \"libavcodec version: {$major}.{$minor}.{$micro}\\n\";\n\n// List available codec configuration\n$config = $libavcodec->avcodec_configuration();\necho \"Configuration: {$config}\\n\";\n\n// Explore codecs: $libavcodec->avcodec_find_encoder(AV_CODEC_ID_H264)\n// then $libavcodec->avcodec_alloc_context3($codec)"
  ],
  "installation": {
    "darwin": {
      "install": ["brew install ffmpeg"],
      "checkIfExists": ["brew list ffmpeg"]
    },
    "linux": {
      "install": ["sudo apt-get install -y libavcodec-dev"],
      "checkIfExists": ["pkg-config --exists libavcodec"]
    }
  },
  "platforms": [
    {
      "os": "darwin",
      "arch": "aarch64"
    },
    {
      "os": "darwin",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "x86_64"
    },
    {
      "os": "linux",
      "arch": "aarch64"
    },
    {
      "os": "windows",
      "arch": "x86_64"
    }
  ],
  "requires": {
    "libavcodec": {
      "symbol_prefix": "avcodec",
      "library_names": [
        "libavcodec.dylib",
        "libavcodec.so",
        "avcodec.dll"
      ],
      "header_names": [
        "libavcodec/avcodec.h"
      ],
      "version": ">=58.0.0 & <62.0.0",
      "required": true
    }
  },
  "dependencies": {}
}
